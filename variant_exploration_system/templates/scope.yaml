# Scope Definition Template
# Maps to: /schema/scope_schema.json
# Defines hypotheses, target markets, and success metrics.

hypothesis: "<Main hypothesis>"
target_segments:
  - "<Primary segment>"
  - "<Secondary segment>"
success_metrics:
  - metric: "Payback < 12 months"
  - metric: "Validated pain >= 60% ICP"
notes: |
  Explain *why* this scope is valid and what assumptions need testing.
