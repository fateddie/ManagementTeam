# Dashboard dependencies (for real-time monitoring UI)
# Install separately with: pip install -r dashboard/requirements.txt
# Or uncomment below to include in main install:
# fastapi==0.104.1
# uvicorn==0.24.0
# streamlit==1.28.1

# Core dependencies
requests
pandas
numpy
pyyaml
python-dotenv
jinja2
jsonschema

# AI & Research
openai
# Note: Perplexity API is accessed via REST API (requests library), not a separate package

# Dashboard & Visualization
streamlit
plotly

# Phase 2: API Integrations
praw  # Reddit API
pytrends  # Google Trends API
tweepy  # X (Twitter) API
textblob  # Sentiment analysis

# Phase 2: ML & Predictions
scikit-learn
joblib

# Phase 3: Social Media Trending Intelligence System
google-api-python-client  # YouTube Data API
transformers  # Hugging Face models (TwHIN-BERT, RoBERTa, BART)
torch  # PyTorch for transformers
sentence-transformers  # Sentence embeddings for BERTopic
bertopic  # Topic modeling with transformers
umap-learn  # Dimensionality reduction for BERTopic
hdbscan  # Clustering for BERTopic

# Phase 16: Persistent Memory System
redis  # In-memory database for persistent agent memory

# Optional future tools
slack_sdk
chromadb
fastapi
uvicorn
